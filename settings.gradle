rootProject.name = 'zipkin-dependencies-spark'

def includes = [
    'cassandra'
]

include includes.toArray(new String[0])

rootProject.children.each { childProject ->
    childProject.name = rootProject.name + '-' + childProject.name
}
